name: products
server:
  host: "0.0.0.0"
  port: 4000
mongo:
  uri: "mongodb://127.0.0.1:27017"
  database: "pizzahub"
logging:
  level: info
limits:
  json_body_bytes: 1048576
cors:
  enabled: true
  origins: ["*"]
